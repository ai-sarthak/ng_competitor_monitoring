add the keywords in csv format:

add the api key

set the max pages to scrape  google news

set max pages to scrape reviews about company

set a min threshold to save a batch of review analysis at once in file to avoid loss of progress in system failure.